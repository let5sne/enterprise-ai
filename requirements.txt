fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.22
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.2
aiosqlite==0.20.0
greenlet==3.1.1

# --- Optional: local RAG (required only when LLM_ENABLED=true) ---
# Install separately on private deployments:
#   pip install chromadb==0.5.23
# Kept optional to keep the baseline install light; VectorRetriever
# imports chromadb lazily so tests without chromadb still pass.
